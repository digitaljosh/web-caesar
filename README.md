# web-caesar

Testing plan:
1. Create a new directory and clone repo to that directory
2. Ensure flask environment is running
3. From command line type 'python main.py'
4. Navigate browser to 'http://localhost:5000'
